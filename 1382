# zoj
因为杭电oj打不开了，估计是要用vpn了，所以被迫做zoj了
#include <stdio.h>
#include <math.h>
int main()
{
	int d, n;
	scanf("%d", &d);
	while (d--)
	{
		scanf("%d", &n);
		int i=0;
		while (n % 2 == 0)
		{
			i++;
			n = n / 2;
		}
		printf("%d %d\n", n, i);
	}

	
		

	return 0;
}
